import { FC } from 'react'

interface IServerSideBarProps {
  serverId: string
}

const ServerSideBar: FC<IServerSideBarProps> = ({ serverId }) => {
  return (
    <div>

    </div>
  )
}

export default ServerSideBar